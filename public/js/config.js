// Zentrale Spiel-Konfiguration.
// Fester Standard-Server für den Mehrspieler-Modus: Wird die App von einer
// statischen Seite (z. B. GitHub Pages) geladen, verbindet sie sich ohne
// weitere Eingabe mit diesem Server. Das Feld "Spiel-Server" kann ihn
// weiterhin überschreiben; kommt die Seite direkt vom Ubongo-Server
// (WLAN/localhost), gilt wie bisher die eigene Herkunft.
export const DEFAULT_SERVER = 'ubongo-server.onrender.com';
